export interface RoutesType {
  login: string;
  register: string;
  dashboard: string;
}

export const Routes: RoutesType = {
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
};
